let form=Document.getElenmentById("user form");
form.addEventListener("submit",(e)=>

{e.preventDefault();
    let name=document.getElementById("name").value;
    let=name=Document.getElenmentById("email").value;
    let=name=Document.getElenmentById("number").value;
    let=name=Document.getElenmentById("passward").value;
    let=name=Document.getElenmentById("confirmpassward").value;
    let obj={
        name,email,phonenumber,passward,confirmPassward
    }
    //console.log(name);
    let tr=Document.createElenment("tr")
    let td1=document.createElenment(td)
    .
      
});
